<h2>Atualizar Produto</h2>
<%= render 'form', product: @product, company: @company, url: client_admin_company_product_path(@company.token, @product.token), button_text: t('.submit') %> 
<%= link_to 'Cancelar', client_admin_company_product_path(@company.token, @product.token), class:"btn btn-dark btn-sm mb-1 mt-1" %>